/*  Programas de computador funcionam manipulando VALORES, como o número 3,14 ou o texto (string) “Olá Mundo”. 

    * VALORES são os dados reais atribuídos às variáveis.
        Os valores podem ser literais (diretamente representados no código) ou resultar de expressões.
        Cada valor tem um TIPO associado.

        Os diferentes valores que podem ser representados e manipulados em uma linguagem de programação são conhecidos como TIPOS 
            e uma das características mais fundamentais de uma linguagem de programação é o conjunto de tipos que ela aceita. 
    
    * TIPOS, em JS, indicam a natureza dos dados armazenados em uma variável.
        JavaScript é dinamicamente tipado, o que significa que o tipo de uma variável pode mudar durante a execução do programa.

    * VARIÁVEL é um nome simbólico associado a um valor ou expressão (are names that represent values in a program).
        Quando um programa precisa manter um valor para uso futuro, ele atribui o valor (ou “armazena” o valor) a uma VARIÁVEL. 
        Uma variável define um nome simbólico para um valor e permite que o valor seja referido pelo nome.
        Usada para armazenar e referenciar dados na memória.
        *** Pode conter um VALOR de qualquer TIPO
        Declaramos variáveis usando as palavras-chave var, let ou const.        
*/

// Variáveis: representam VALORES em um programa.

// Valores: dados reais atribuídos às variáveis.

// Tipos: indicam a natureza dos dados armazenados em uma variável